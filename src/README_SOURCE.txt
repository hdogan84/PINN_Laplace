The whole source folder is not used at all since all jupyter notebooks are designed in a way that allows for immediate usage.
Inside the notebooks, paths are configured to store all relevant outputs in the github repo.
Therefore no source code is needed.